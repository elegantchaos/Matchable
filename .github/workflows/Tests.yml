# --------------------------------------------------------------------------------
# This workflow was automatically generated by ActionBuilderTool 2.1.0 (90).
# (see https://github.com/elegantchaos/ActionBuilderCore for more details)
# --------------------------------------------------------------------------------

name: Tests

on: [push, pull_request]

jobs:

    macOS-swift59:
        name: macOS (Swift 5.9)
        runs-on: macos-13
        steps:
        - name: Checkout
          uses: actions/checkout@v4
        - name: Install xcbeautify
          run: brew install xcbeautify
        - name: Make Logs Directory
          run: mkdir logs
        - name: Select Swift
          uses: beeauvin/swiftly-swift@v1
          with:
            swift-version: "5.9"
        - name: Swift Version
          run: swift --version
        - name: Build (release)
          run: swift build --configuration release --quiet
        - name: Test (release)
          run: |
            set -o pipefail
            swift test --configuration release | xcbeautify --quiet --disable-logging --renderer github-actions


    macOS-swift61:
        name: macOS (Swift 6.1)
        runs-on: macos-15
        steps:
        - name: Checkout
          uses: actions/checkout@v4
        - name: Install xcbeautify
          run: brew install xcbeautify
        - name: Make Logs Directory
          run: mkdir logs
        - name: Select Swift
          uses: beeauvin/swiftly-swift@v1
          with:
            swift-version: "6.1"
        - name: Swift Version
          run: swift --version
        - name: Build (release)
          run: swift build --configuration release --quiet
        - name: Test (release XCTest)
          run: |
            set -o pipefail
            swift test --disable-swift-testing --configuration release | xcbeautify --quiet --disable-logging --renderer github-actions
        - name: Test (release Swift Testing)
          run: |
            set -o pipefail
            swift test --disable-xctest --configuration release | xcbeautify --quiet --disable-logging --renderer github-actions


    xcode-swift59:
        name: iOS/tvOS/visionOS/watchOS (Swift 5.9, Xcode 15.2)
        runs-on: macos-13
        steps:
        - name: Checkout
          uses: actions/checkout@v4
        - name: Install xcbeautify
          run: brew install xcbeautify
        - name: Make Logs Directory
          run: mkdir logs
        - name: Select Xcode Version
          run: |
            ls -d /Applications/Xcode* > logs/xcode-versions.log
            sudo xcode-select -s /Applications/Xcode_15.2.app
            xcodebuild -version
            swift --version
        - name: Detect Workspace & Scheme (iOS)
          run: |
            WORKSPACE="Matchable.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
              WORKSPACE="."
              GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Matchable-Package || true))
              if [[ $GOTPACKAGE != "" ]]
              then
                SCHEME="Matchable-Package"
              else
                SCHEME="Matchable"
              fi
            else
              SCHEME="Matchable-iOS"
            fi
            echo "set -o pipefail; export PATH='swift-latest:$PATH'; WORKSPACE='$WORKSPACE'; SCHEME='$SCHEME'; xcodebuild -downloadPlatform iOS > logs/download-iOS.log" > setup.sh
        - name: Test (iOS Release)
          run: |
            source "setup.sh"
            echo "Testing workspace $WORKSPACE scheme $SCHEME."
            set -o pipefail
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=iPhone SE (3rd generation)" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-iOS-test-release.log | xcbeautify --quiet --disable-logging --renderer github-actions
        - name: Detect Workspace & Scheme (tvOS)
          run: |
            WORKSPACE="Matchable.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
              WORKSPACE="."
              GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Matchable-Package || true))
              if [[ $GOTPACKAGE != "" ]]
              then
                SCHEME="Matchable-Package"
              else
                SCHEME="Matchable"
              fi
            else
              SCHEME="Matchable-tvOS"
            fi
            echo "set -o pipefail; export PATH='swift-latest:$PATH'; WORKSPACE='$WORKSPACE'; SCHEME='$SCHEME'; xcodebuild -downloadPlatform tvOS > logs/download-tvOS.log" > setup.sh
        - name: Test (tvOS Release)
          run: |
            source "setup.sh"
            echo "Testing workspace $WORKSPACE scheme $SCHEME."
            set -o pipefail
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple TV 4K (3rd generation)" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-tvOS-test-release.log | xcbeautify --quiet --disable-logging --renderer github-actions
        - name: Detect Workspace & Scheme (visionOS)
          run: |
            WORKSPACE="Matchable.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
              WORKSPACE="."
              GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Matchable-Package || true))
              if [[ $GOTPACKAGE != "" ]]
              then
                SCHEME="Matchable-Package"
              else
                SCHEME="Matchable"
              fi
            else
              SCHEME="Matchable-visionOS"
            fi
            echo "set -o pipefail; export PATH='swift-latest:$PATH'; WORKSPACE='$WORKSPACE'; SCHEME='$SCHEME'; xcodebuild -downloadPlatform visionOS > logs/download-visionOS.log" > setup.sh
        - name: Test (visionOS Release)
          run: |
            source "setup.sh"
            echo "Testing workspace $WORKSPACE scheme $SCHEME."
            set -o pipefail
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple Vision Pro" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-visionOS-test-release.log | xcbeautify --quiet --disable-logging --renderer github-actions
        - name: Detect Workspace & Scheme (watchOS)
          run: |
            WORKSPACE="Matchable.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
              WORKSPACE="."
              GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Matchable-Package || true))
              if [[ $GOTPACKAGE != "" ]]
              then
                SCHEME="Matchable-Package"
              else
                SCHEME="Matchable"
              fi
            else
              SCHEME="Matchable-watchOS"
            fi
            echo "set -o pipefail; export PATH='swift-latest:$PATH'; WORKSPACE='$WORKSPACE'; SCHEME='$SCHEME'; xcodebuild -downloadPlatform watchOS > logs/download-watchOS.log" > setup.sh
        - name: Build (watchOS release)
          run: |
            source "setup.sh"
            echo "Building workspace $WORKSPACE scheme $SCHEME."
            set -o pipefail
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple Watch Series 10 (42mm)" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-watchOS-build-release.log | xcbeautify --quiet --disable-logging --renderer github-actions
        - name: Upload Logs
          uses: actions/upload-artifact@v4
          if: always()
          with:
            name: logs
            path: logs


    xcode-swift61:
        name: iOS/tvOS/visionOS/watchOS (Swift 6.1, Xcode 16.4.0)
        runs-on: macos-15
        steps:
        - name: Checkout
          uses: actions/checkout@v4
        - name: Install xcbeautify
          run: brew install xcbeautify
        - name: Make Logs Directory
          run: mkdir logs
        - name: Select Xcode Version
          run: |
            ls -d /Applications/Xcode* > logs/xcode-versions.log
            sudo xcode-select -s /Applications/Xcode_16.4.0.app
            xcodebuild -version
            swift --version
        - name: Detect Workspace & Scheme (iOS)
          run: |
            WORKSPACE="Matchable.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
              WORKSPACE="."
              GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Matchable-Package || true))
              if [[ $GOTPACKAGE != "" ]]
              then
                SCHEME="Matchable-Package"
              else
                SCHEME="Matchable"
              fi
            else
              SCHEME="Matchable-iOS"
            fi
            echo "set -o pipefail; export PATH='swift-latest:$PATH'; WORKSPACE='$WORKSPACE'; SCHEME='$SCHEME'; xcodebuild -downloadPlatform iOS > logs/download-iOS.log" > setup.sh
        - name: Test (iOS Release)
          run: |
            source "setup.sh"
            echo "Testing workspace $WORKSPACE scheme $SCHEME."
            set -o pipefail
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=iPhone SE (3rd generation)" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-iOS-test-release.log | xcbeautify --quiet --disable-logging --renderer github-actions
        - name: Detect Workspace & Scheme (tvOS)
          run: |
            WORKSPACE="Matchable.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
              WORKSPACE="."
              GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Matchable-Package || true))
              if [[ $GOTPACKAGE != "" ]]
              then
                SCHEME="Matchable-Package"
              else
                SCHEME="Matchable"
              fi
            else
              SCHEME="Matchable-tvOS"
            fi
            echo "set -o pipefail; export PATH='swift-latest:$PATH'; WORKSPACE='$WORKSPACE'; SCHEME='$SCHEME'; xcodebuild -downloadPlatform tvOS > logs/download-tvOS.log" > setup.sh
        - name: Test (tvOS Release)
          run: |
            source "setup.sh"
            echo "Testing workspace $WORKSPACE scheme $SCHEME."
            set -o pipefail
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple TV 4K (3rd generation)" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-tvOS-test-release.log | xcbeautify --quiet --disable-logging --renderer github-actions
        - name: Detect Workspace & Scheme (visionOS)
          run: |
            WORKSPACE="Matchable.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
              WORKSPACE="."
              GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Matchable-Package || true))
              if [[ $GOTPACKAGE != "" ]]
              then
                SCHEME="Matchable-Package"
              else
                SCHEME="Matchable"
              fi
            else
              SCHEME="Matchable-visionOS"
            fi
            echo "set -o pipefail; export PATH='swift-latest:$PATH'; WORKSPACE='$WORKSPACE'; SCHEME='$SCHEME'; xcodebuild -downloadPlatform visionOS > logs/download-visionOS.log" > setup.sh
        - name: Test (visionOS Release)
          run: |
            source "setup.sh"
            echo "Testing workspace $WORKSPACE scheme $SCHEME."
            set -o pipefail
            xcodebuild test -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple Vision Pro" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-visionOS-test-release.log | xcbeautify --quiet --disable-logging --renderer github-actions
        - name: Detect Workspace & Scheme (watchOS)
          run: |
            WORKSPACE="Matchable.xcworkspace"
            if [[ ! -e "$WORKSPACE" ]]
            then
              WORKSPACE="."
              GOTPACKAGE=$(xcodebuild -workspace . -list | (grep Matchable-Package || true))
              if [[ $GOTPACKAGE != "" ]]
              then
                SCHEME="Matchable-Package"
              else
                SCHEME="Matchable"
              fi
            else
              SCHEME="Matchable-watchOS"
            fi
            echo "set -o pipefail; export PATH='swift-latest:$PATH'; WORKSPACE='$WORKSPACE'; SCHEME='$SCHEME'; xcodebuild -downloadPlatform watchOS > logs/download-watchOS.log" > setup.sh
        - name: Build (watchOS release)
          run: |
            source "setup.sh"
            echo "Building workspace $WORKSPACE scheme $SCHEME."
            set -o pipefail
            xcodebuild clean build -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "name=Apple Watch Series 10 (42mm)" -configuration Release CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ENABLE_TESTABILITY=YES | tee logs/xcodebuild-watchOS-build-release.log | xcbeautify --quiet --disable-logging --renderer github-actions
        - name: Upload Logs
          uses: actions/upload-artifact@v4
          if: always()
          with:
            name: logs
            path: logs

